apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "three-tier-pgsql.fullname" . }}-test-connection"
  labels:
    {{- include "three-tier-pgsql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "three-tier-pgsql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
