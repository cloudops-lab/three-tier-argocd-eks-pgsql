apiVersion: v1
kind: Service
metadata:
  name: three-tier-backend
  labels:
    app: three-tier-backend
spec:
  type: ClusterIP
  selector:
    app: three-tier-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
